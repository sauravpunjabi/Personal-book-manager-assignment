# Optional — hosting platforms set this themselves. Defaults to 5000 locally.
PORT=5000
MONGODB_URI=mongodb+srv://...
JWT_SECRET=your_jwt_secret_minimum_32_chars
# Comma-separate to allow more than one, e.g. local plus the deployed site
CLIENT_ORIGIN=http://localhost:3000
NODE_ENV=development
